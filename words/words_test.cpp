// use google test
// make rule into a test vector

int word_test() {
//    assert (compare("", ""), false); // "" is not a word
//    assert (compare("word", "word", rule), false); // "" is not a word
//    assert (compare("Word", "word", rule), true); // rule is case insensitive
//    assert (compare("Word", "word", rule2), false); // rule2 is case sensitive
//    assert (compare("Word ", "Word", rule), true); // space are not valid word characters
//
//    assert (distance("word", "word", rule), 0); // "" is not a word
//    assert (distance("Word", "word", rule), 0); // rule is case insensitive
//    assert (distance("Word", "word", rule2), 1); // rule2 is case sensitive
//    assert (distance("Word", "worD", rule2), 2); // rule2 is case sensitive
//    assert (distance("Word ", "Word"), 0); // space are not valid word characters
//
//    assert (distance("wood", "word", rule), 1); // one letter change
//    assert (distance("Wood", "word", rule), 1); // rule2 is case insensitive
//    assert (distance("Wood", "word", rule2), 2); // rule2 is case sensitive
//    assert (distance("Word ", "Word"), 0); // space are not valid word characters
//
//    assert (distance("word", "word", rule), 0); // "" is not a word
//    assert (distance("Word", "word", rule), 0); // rule is case insensitive
//    assert (distance("Word", "word", rule2), 1); // rule2 is case sensitive
//    assert (distance("Word ", "Word"), 0); // space are not valid word characters
}
